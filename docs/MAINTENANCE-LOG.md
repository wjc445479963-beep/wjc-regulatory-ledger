# wjc 的韭菜花园维护记录

这份记录用于说明法规主库和网站功能的历次调整，避免后续维护时忘记数据从哪里来、为什么这样处理。

## 2026-08-27：初始法规主库与版本比对

- 建立法规合规台账主库，统一保存法规名称、标准号、来源、分类、生效/关注日期、状态和官方来源链接。
- 保留现行版本、即将实施版本和必要的替代关系；已被替代版本只用于识别旧清单，不作为公开主库版本展示。
- 增加 `YY/T 0466.2` 到主库。
- 增加法规版本比对的标准号匹配、名称匹配、替代版本识别、即将实施版本识别和未匹配结果。
- 增加比对结果状态卡片筛选和比对结果导出。

## 2026-08-28：补录四份比对结果中的法规

- 根据四份《法规版本比对结果》文件，将其中已识别但原主库未收录的记录补录到 `app/regulations-data.ts`。
- 本次补录共 313 条，使用 `imported-compare-*` 作为记录标识，来源说明统一注明为“四份法规版本比对结果”。
- 无法仅凭比对文件明确确认现行状态的记录继续标记为“待核对”，不把推测结果伪装成官方结论。
- 主库保留必要的“已被替代”记录用于版本比对；公开清单和公开导出不展示已被替代版本。
- 增加比对结果的原清单版本号、发布日期、实施日期、来源、备注、主库参考状态、最后核对日期、来源依据和官方链接等溯源字段。
- 增加主库 Excel 导出结构，按现行法规、即将实施和法规类别拆分工作表；待核对记录仅保留给后续维护使用。
- 增加本地预览中的批量核对辅助功能；批量确认在静态版本中不作为网站永久写入，最终状态以 GitHub 中的主库源文件为准。

## 2026-08-29：静态网站改造

- 取消法规页面对运行时 ChatGPT 登录信息的依赖。
- 取消法规页面对运行时数据库的依赖；法规数据直接随网站构建发布。
- 保留游客可用的法规查询、筛选、详情、版本比对和浏览器端导出功能。
- 将待核对记录留在源数据中，公开网页只展示现行和即将实施版本。
- 增加 GitHub Pages 构建与自动发布配置，后续每次推送 `main` 分支都会重新构建静态网站。

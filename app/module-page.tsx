import Link from "next/link";
import { ArrowLeft, ArrowRight } from "lucide-react";
import { sitePath } from "@/lib/site-path";

export default function ModulePage({ eyebrow, title, description, color, items }: { eyebrow: string; title: string; description: string; color: string; items: string[] }) {
  return <main className="min-h-screen bg-[#f6f8fb] text-[#10213d]"><header className="border-b border-slate-200 bg-[#10213d] px-6 py-5 text-white"><div className="mx-auto max-w-[1100px]"><Link href={sitePath("/")} className="inline-flex items-center gap-2 text-sm text-slate-300 hover:text-white"><ArrowLeft className="size-4" />返回工作站首页</Link></div></header><div className="mx-auto max-w-[1100px] px-6 py-12 lg:py-16"><div className={`rounded-2xl border bg-white p-8 md:p-12 ${color}`}><p className="text-xs font-semibold uppercase tracking-[0.2em] text-slate-400">{eyebrow}</p><h1 className="mt-4 text-4xl font-semibold tracking-tight">{title}</h1><p className="mt-5 max-w-2xl text-base leading-8 text-slate-600">{description}</p><div className="mt-10 grid gap-3 md:grid-cols-2">{items.map((item) => <div key={item} className="flex items-center gap-3 rounded-xl bg-slate-50 px-4 py-4 text-sm text-slate-600"><span className="size-2 rounded-full bg-[#e7655e]" />{item}</div>)}</div><Link href={sitePath("/")} className="mt-10 inline-flex items-center gap-2 rounded-lg bg-[#10213d] px-4 py-2.5 text-sm font-semibold text-white hover:bg-[#18345d]">返回模块导航 <ArrowRight className="size-4" /></Link></div></div></main>;
}
